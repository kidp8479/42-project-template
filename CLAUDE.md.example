# <PROJECT NAME> - 42 post-tronc-commun

<!-- Copy this file to CLAUDE.md and fill it in.

     CLAUDE.md is gitignored by default (agent working notes stay
     local). Once the project is underway, track it deliberately with
     `git add -f CLAUDE.md` if it needs to survive a `git clone` on
     another machine (home / school) - see .claude/standards/school-42.md
     "Portability" for why that is safe to do.

     Right before the final defense, both CLAUDE.md and .claude/ get
     removed from the branch that is evaluated - see
     .claude/standards/school-42.md "Defense preparation". They are
     working tooling, not a deliverable.

     See Hypertube's CLAUDE.md for a worked example.

     This file holds ONLY subject-specific context: stack, eliminatory
     constraints, imposed structure, open questions. Project-agnostic
     engineering practice is in .claude/standards/engineering.md; the 42
     checklist to instantiate below is in .claude/standards/school-42.md.
     Don't duplicate either here - reference them. -->

## Context

- 42 slug `<slug>`, `<XP>` XP, category `<category>`.
- RNCP contribution: `<Titre / Option / sub-category>`, thresholds to
  re-confirm on meta.intra.42.fr.
- Subject PDF: `<path>` (version `<X>`, dated - re-confirm exact rules on
  the intra before the final defense; this file is a working summary).
- Why this project / relevant student context: `<...>`

## Stack

- Decided: `<...>` (each with an ADR under `docs/adr/`)
- Not frozen yet: `<...>`

## Eliminatory constraints (0 if violated)

<!-- Copied verbatim from the subject AND the marking sheet, both read in
     full. Where they diverge, the stricter applies. See
     .claude/standards/school-42.md for the common web list. -->

- `<...>`

## 42 process checklist

<!-- From .claude/standards/school-42.md, instantiated. -->

- Repo visibility: `<public / private>`
- Login in repo name: `<yes / no>`
- Imposed directory structure: `<describe / n-a>`
- Evaluation runs on: `<the evaluated group's machine / other>`
- Solo or team: `<impact on VM and repo naming>`
- Seeded test accounts: `<account>` / `<where the password lives>`

## Engineering standards

Project-agnostic standards are in `.claude/standards/engineering.md` and
apply unchanged. Below: only project-specific deviations or additions
(e.g. a stricter tenant-isolation rule, a framework-specific guard
convention).

- `<...>`

## Not yet decided / to settle at session start

- `<...>`

## Don't forget

- Re-confirm the exact subject rules on the intra before the final
  defense.
- Check meta.intra.42.fr for the RNCP thresholds before treating this
  project as "acquired" for its sub-category.
